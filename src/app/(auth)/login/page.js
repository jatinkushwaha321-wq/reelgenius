import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In — NIVO',
  description: 'Return to your NIVO intelligence environment.',
};

export default function LoginPage() {
  return <LoginForm />;
}
